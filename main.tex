\documentclass[12pt,a4paper]{report}

\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{svg}
\usepackage{geometry}
\geometry{a4paper, margin=1in}
\usepackage{titlesec}
\usepackage[hypertexnames=false]{hyperref}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{float}
\usepackage{listings}
\usepackage{xcolor}

\lstdefinelanguage{json}{
  morestring=[b]",
  sensitive=false,
  breaklines=true,
  columns=fullflexible,
  showstringspaces=false
}

\lstdefinestyle{chaptercode}{
  basicstyle=\footnotesize\ttfamily\color{black},
  keywordstyle=\bfseries\color{black},
  commentstyle=\itshape\color{black},
  stringstyle=\color{black},
  backgroundcolor=\color{white},
  frame=single,
  rulecolor=\color{black},
  framesep=5pt,
  breaklines=true,
  breakatwhitespace=false,
  breakindent=1.5em,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  tabsize=4,
  numbers=left,
  numberstyle=\tiny\color{black},
  numbersep=7pt,
  xleftmargin=1.8em,
  framexleftmargin=1.4em,
  aboveskip=0.8em,
  belowskip=0.8em
}

\lstset{style=chaptercode}

\emergencystretch=2em
\pdfsuppresswarningpagegroup=1

\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries}{\chaptertitlename\ \thechapter}{20pt}{\LARGE}

\begin{document}

\begin{titlepage}
    \centering
    \vspace*{0.4cm}
    
    {\Large \textsc{B.Sc. Engg. Project}}\\[1cm]
    
    {\huge \textbf{SELLWISE (Predictive Sales \& Inventory Analytics)}}\\[1.5cm]
    
    {\Large Developed by}\\[0.35cm]
    {\large
    \begin{tabular}{c}
        Md. Sohel Rana (ID: 222351083) \\
        Md. Labu Miah (ID: 222351215) \\
        Faysal Islam Fahad (ID: 222351133) \\
        Naushin Sultana Mim (ID: 222351203) \\
        Milhan Jannat Jarin (ID: 222351125)
    \end{tabular}}\\[0.8cm]
    
    {\Large Supervised by}\\[0.3cm]
    {\large Humaira Ahmed}\\[0.15cm]
    {\large Assistant Professor}\\[0.8cm]
    
    {\Large Department of Computer Science \& Engineering}\\[0.3cm]
    {\large (In partial fulfillment of the requirements for the degree of\\
    Bachelor of Science in Computer Science \& Engineering)}\\[0.8cm]
    
    \vfill

    \includegraphics[height=5cm]{diagrams/bubt_logo.png}\\[0.2cm]

    {\Large Department of Computer Science \& Engineering}\\[0.3cm]
    {\Large Bangladesh University of Business \& Technology (BUBT)}\\[0.3cm]
    {\large Rupnagar, Mirpur-2, Dhaka-1216, Bangladesh}\\[0.3cm]
    {\large \today}
\end{titlepage}

\pagenumbering{roman}
\tableofcontents
\newpage
\listoffigures
\newpage
\pagenumbering{arabic}

\chapter{INTRODUCTION}
\section{Introduction}
SellWise is a web application designed to help retail businesses manage their daily operations more effectively. It combines sales processing, inventory management, and customer tracking into one system. Even today, many small and medium-sized shops use paper registers or basic spreadsheets to keep track of their business. These manual methods are slow, prone to mistakes, and difficult to manage as the business grows. SellWise replaces these older methods with a reliable digital system to keep operations organized.

In addition to handling daily transactions, SellWise uses machine learning to predict how much of a product will be needed in the upcoming weeks. The system is divided into two main parts: an ASP.NET Core web application that manages users and sales, and a separate Python service that runs the demand forecasting. Keeping these two parts separate ensures that the main website remains fast and responsive, even when complex calculations are happening in the background.

\section{Project overview}
This project provides a practical and affordable software solution for small to medium-sized retail shops. It is built with a simple user interface so that shop owners and employees can use it without needing advanced technical skills. 

SellWise uses a multi-tenant design, which means that each shop gets its own secure workspace and data from different stores is kept completely separate. By handling the difficult tasks of record-keeping, tracking stock, and predicting future sales, the system allows shop owners to focus on growing their business instead of managing paperwork.

\section{Problem Statement}
Without proper management software, small businesses face several common problems. Depending on manual records or disconnected tools usually leads to:
\begin{itemize}
    \item \textbf{Inventory Issues:} Shops often tie up their money by overstocking items that are not selling well, or they run out of popular items and lose potential sales.
    \item \textbf{Losing Customers:} Because store owners cannot easily track what customers are buying over time, they often fail to notice when a regular customer stops visiting.
    \item \textbf{Scattered Information:} Keeping sales records in one place and inventory in another makes it very difficult to calculate the actual profit or loss of the business.
    \item \textbf{No Future Planning:} Traditional methods only show what has already happened. Owners do not have the tools to predict what products they will need to order for the next month.
\end{itemize}

\section{Motivation}
Seeing the struggles that small shop owners face every day motivated us to build SellWise. We wanted to create a system that brings advanced analytics to small businesses. Our main motivations are to:
\begin{itemize}
    \item Give shop owners a clear, single dashboard to monitor their entire business.
    \item Help businesses plan their inventory accurately by forecasting future demand.
    \item Track customer buying habits automatically to improve customer service.
    \item Provide an affordable and efficient alternative to manual record-keeping.
    \item Prevent data loss and the calculation errors that frequently occur with paper records.
\end{itemize}

\section{Objectives}
\begin{itemize}
    \item Build a responsive and easy-to-use web platform for managing sales and stock.
    \item Implement a machine learning model to forecast product demand for the next 30 days.
    \item Ensure that database transactions are processed safely, so inventory data is never corrupted during a sale.
    \item Automatically group customers based on their purchase history using the RFM (Recency, Frequency, Monetary) method.
    \item Create an automated alert system that warns managers when a product's stock is running low.
    \item Generate downloadable PDF reports that summarize daily and monthly sales.
    \item Reduce the amount of manual paperwork required to run a shop.
\end{itemize}

\section{Development Tools}
SellWise is built using a combination of a relational database, a web framework, and a machine learning service.
\begin{itemize}
    \item \textbf{Technology:}
    \begin{itemize}
        \item SQL Server (running in Docker) for the database.
        \item ASP.NET Core 10 MVC for the main web application.
        \item FastAPI (Python) for the machine learning API.
        \item Meta Prophet for time-series forecasting.
        \item Entity Framework Core 10 for database interactions.
    \end{itemize}
    \item \textbf{Languages:}
    \begin{itemize}
        \item C\#
        \item Python
        \item HTML5, CSS3 (Bootstrap 5), and JavaScript
        \item SQL
    \end{itemize}
\end{itemize}

\section{Proposed System}
The proposed system includes three main types of users: Business Owner, Store Manager, and Employee. Users log into the system with their email and password. A single owner account can manage multiple store locations from one place.

The \textbf{Owner} has full control over all settings and reports. The \textbf{Manager} can process sales, check inventory levels, and view business analytics. The \textbf{Employee} is only allowed to process sales orders and view the product list.

When an order is placed, the system immediately checks the inventory. If the stock is too low, it prevents the sale or shows a warning. At the same time, a background service looks at past sales data to predict future demand, which is displayed on the owner's dashboard.

\section{Purpose Of The System}
Many small shop owners know they need better software, but they cannot afford the expensive systems used by large companies. The purpose of SellWise is to make these advanced tools accessible to small businesses.

By putting the point-of-sale, customer tracking, and inventory management into one application, owners do not have to switch between different programs. The system handles the complex math—like figuring out which customers are the most loyal and predicting what will sell next month—so small businesses have a better chance to compete and grow.

\section{Goal}
The main goal of this project is to create an easy-to-use system that makes running a retail shop much more efficient. Another important goal is to improve how owners make decisions by giving them accurate data and future predictions. Ultimately, we want to provide a reliable online platform so that business owners can check their store's performance from anywhere at any time.

\chapter{EXISTING SYSTEM}

\section{Introduction}
Using technology is now essential for a business to grow. While many local shops still use manual methods, using proper software increases speed and reduces mistakes. This chapter analyzes existing retail software to understand what features are necessary. It also looks at the tools currently available in the market to identify their limitations, especially why small shops often avoid using them.

\section{Existing System}
We examined several popular retail management systems. The details are discussed below.

\subsection{Shopify Analytics}
Shopify is a well-known e-commerce platform that includes built-in analytics. It helps sellers track their sales, website visitors, and inventory.

\textbf{Advantages:}
\begin{itemize}
    \item Very easy to use with clear dashboard designs.
    \item Connects online stores with physical point-of-sale systems.
    \item Tracks inventory and sends basic low-stock warnings.
\end{itemize}

\textbf{Disadvantages:}
\begin{itemize}
    \item The more advanced analytics features require expensive monthly plans.
    \item It does not have built-in machine learning to predict future product demand.
    \item Users often have to buy third-party apps to get all the features they need.
\end{itemize}

\subsection{Square for Retail}
Square provides a popular point-of-sale system for small businesses that includes basic inventory and reporting features.

\textbf{Advantages:}
\begin{itemize}
    \item The basic point-of-sale and inventory tracking software is free.
    \item Provides reliable sales reports and history.
    \item Keeps a directory of customers and what they have bought.
\end{itemize}

\textbf{Disadvantages:}
\begin{itemize}
    \item Detailed analytics are only available if you pay for the Square Analytics upgrade.
    \item It does not predict future sales trends automatically.
    \item It can be difficult to manage multiple store locations using the free version.
\end{itemize}

\subsection{Lightspeed Retail}
Lightspeed is a more advanced retail system made for detailed inventory management and data analysis.

\textbf{Advantages:}
\begin{itemize}
    \item Very detailed inventory tracking, which is great for stores with multiple branches.
    \item Provides excellent custom reports and sales analytics.
    \item Includes tools for managing suppliers and creating purchase orders.
\end{itemize}

\textbf{Disadvantages:}
\begin{itemize}
    \item The price is generally too high for small startups.
    \item It lacks machine learning tools for automated demand forecasting.
    \item The system is complex, meaning it takes a lot of time to set up and train staff.
\end{itemize}

\section{Supporting Literature}

\subsection{Used Diagram}

\textbf{Entity Relationship Diagram (ERD)}\\
An Entity-Relationship model shows the logical structure of a database using a diagram. It acts as a blueprint for how data is stored. The main parts are:
\begin{itemize}
    \item \textbf{Entity:} An object or concept (like a User or Product) stored in the database. In a database, this is usually a table.
    \item \textbf{Attribute:} The details or properties that describe the entity.
    \item \textbf{Relationship:} Shown as a diamond shape, this describes how different entities interact with each other (for example, a Customer makes an Order).
\end{itemize}

\textbf{Data Flow Diagram (DFD)}\\
A Data Flow Diagram shows how data moves through a system. It shows where the data comes from, where it goes, and where it is stored. The main parts are:
\begin{itemize}
    \item \textbf{Process:} An action that takes input data and changes it into output data.
    \item \textbf{Data Flow:} The path the data takes between processes and storage.
    \item \textbf{Data Store:} A place where data is kept so it can be used later.
    \item \textbf{External Entity:} People or other systems outside the software that send or receive data.
\end{itemize}

\textbf{Use Case Diagram}\\
A Use Case Diagram gives a simple overview of what the system does from the user's point of view. It shows how people interact with the software.
\begin{itemize}
    \item \textbf{System:} The boundary that contains all the actions the software can perform.
    \item \textbf{Actors:} The people (like the Owner or Manager) who use the system.
\end{itemize}

\subsection{Technology Used}
To build SellWise, we used ASP.NET Core for the web application, HTML, CSS, and JavaScript for the user interface, and Python for the machine learning features.
\begin{itemize}
    \item \textbf{ASP.NET Core MVC:} This framework was used to build the main website. It separates the database code (Model), the user interface (View), and the logic (Controller).
    \item \textbf{Entity Framework Core:} This tool allows us to interact with the SQL Server database using C\# code instead of writing complex SQL queries.
    \item \textbf{Python FastAPI:} We used this fast web framework to create the machine learning service as a separate API.
    \item \textbf{Meta Prophet:} An open-source forecasting tool used to predict future sales based on past data.
    \item \textbf{Bootstrap 5 \& Chart.js:} These tools were used to make the website look good on all devices and to draw the graphs on the dashboard.
\end{itemize}

\section{Conclusion}
While there are many software options for retail management, most of them are either too basic or too expensive and complicated. SellWise fills this gap by offering advanced features—like predicting sales and grouping loyal customers—in a package that is simple and affordable for small businesses.

\chapter{SYSTEM EVALUATION}

\section{Introduction}
Retail management software covers many areas, from simply recording sales to complex online reporting. After seeing what was missing in basic software, we built SellWise using modern web tools and machine learning. By combining sales records, inventory warnings, and future predictions into one system, we aim to give shop owners tools that are easy to use and actually help them make better business choices.

\section{Requirement Analysis}
Requirement Analysis is the process of defining exactly what the software needs to do before starting to build it. It makes sure the final product meets the users' needs.

\subsection{Non-Functional Requirements}
Non-functional requirements describe how well the system should perform its tasks:
\begin{itemize}
    \item \textbf{Performance:} The dashboard must load quickly, and saving an order should take only a few seconds.
    \item \textbf{Security:} Passwords must be encrypted, and users can only access pages allowed by their specific role (Owner, Manager, or Employee).
    \item \textbf{Reliability:} If the system crashes while an order is being saved, the database must not be corrupted.
    \item \textbf{Manageability:} The code should be organized so that it is easy to update in the future.
    \item \textbf{Usability:} The website must adapt to different screen sizes, working well on both computers and mobile phones.
\end{itemize}

\subsection{Functional Requirements}
Functional requirements list the specific features the system must have:
\begin{itemize}
    \item Users must be able to register, log in, and log out.
    \item The system must allow users to add, edit, and delete products.
    \item Users can add items to a cart and complete a sales order.
    \item The system must have a dashboard showing sales statistics and graphs.
    \item The dashboard must show predicted future demand for products.
    \item The system must alert the user when a product is running low on stock.
    \item The system must save customer details and calculate their loyalty score.
    \item Users must be able to download sales reports as PDF files.
\end{itemize}

\section{Methodology}

\subsection{Agile Development Model}
For this project, we used the Agile Development Model. Agile focuses on building the software in small, manageable pieces and making improvements based on testing and feedback.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth,height=0.5\textheight,keepaspectratio]{diagrams/agile.png}
    \caption{Agile Development Cycle}
\end{figure}

\textbf{Reasons for choosing the Agile Model:}
\begin{itemize}
    \item It allows us to build and test features one by one, making it easier to fix mistakes early.
    \item It is flexible, so if we needed to change a requirement during development, we could do so easily.
    \item It encourages regular communication and testing throughout the project.
    \item Bugs are found and fixed during the development cycle rather than waiting until the very end.
\end{itemize}

\clearpage
\subsection{Gantt Chart}
\begin{figure}[H]
    \centering
    \includesvg[inkscapelatex=false,width=0.8\textwidth,height=0.4\textheight,keepaspectratio]{diagrams/gantt_chart.svg}
    \caption{Project Gantt Chart}
\end{figure}

\subsection{Data Flow Diagram}
\begin{figure}[H]
    \centering
    \includesvg[inkscapelatex=false,width=0.78\textwidth,height=0.4\textheight,keepaspectratio]{diagrams/dfd_level0.svg}
    \caption{Context Level DFD (Level 0)}
\end{figure}

\begin{figure}[H]
    \centering
    \includesvg[inkscapelatex=false,width=\textwidth,height=0.85\textheight,keepaspectratio]{diagrams/dfd_level1.svg}
    \caption{Level 1 Data Flow Diagram}
\end{figure}

\subsection{ER Diagram}
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,height=0.85\textheight,keepaspectratio]{"diagrams/ER Diagram.drawio.pdf"}
    \caption{Entity Relationship Diagram}
\end{figure}

\textbf{Key Tables:}
\begin{itemize}
    \item Users and Stores: User, Store, StoreMember
    \item Sales Data: Product, Order, OrderItem
    \item Other Records: Customer, Expense, InventoryAlert, Forecast
\end{itemize}

\subsection{Use Case Diagram}
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,height=0.85\textheight,keepaspectratio]{"diagrams/USE CASE Digram.drawio.pdf"}
    \caption{Use Case Diagram}
\end{figure}

\begin{itemize}
    \item \textbf{Owner:} Can manage staff, view the dashboard, switch between stores, and download reports.
    \item \textbf{Manager:} Can process orders, update inventory, and manage customer profiles.
    \item \textbf{Employee:} Can only process sales and look at the product list.
\end{itemize}

\subsection{Activity Diagram}
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,height=0.85\textheight,keepaspectratio]{"diagrams/Activity Diagram.drawio.pdf"}
    \caption{Activity Diagram for Order Processing}
\end{figure}

\textbf{How an order is processed:}\\
The user clicks "Create Order" $\rightarrow$ Adds items to the cart $\rightarrow$ Selects a customer $\rightarrow$ Submits the form $\rightarrow$ The system starts a database transaction $\rightarrow$ It checks if there is enough stock $\rightarrow$ If yes, it reduces the stock and saves the order $\rightarrow$ The transaction is completed successfully.

\subsection{Sequence Diagram}
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,height=0.85\textheight,keepaspectratio]{"diagrams/Sequence Diagram.drawio.pdf"}
    \caption{Sequence Diagram for the Dashboard}
\end{figure}

\textbf{How the dashboard loads data:}\\
The browser requests the dashboard page $\rightarrow$ AnalyticsService checks the database for past sales $\rightarrow$ It checks if a recent forecast is saved $\rightarrow$ If not, it sends a request to the Python ML Service $\rightarrow$ Python calculates the prediction and sends it back $\rightarrow$ AnalyticsService saves the new prediction $\rightarrow$ The dashboard is displayed to the user.

\subsection{Class Diagram}
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,height=0.85\textheight,keepaspectratio]{"diagrams/Class Diagram.drawio.pdf"}
    \caption{Class Diagram}
\end{figure}

\begin{itemize}
    \setlength{\itemsep}{0pt}
    \setlength{\parskip}{0pt}
    \setlength{\topsep}{4pt}
    \item \textbf{BaseController:} Checks which store the user is currently logged into.
    \item \textbf{DashboardController and OrderController:} Use the BaseController to ensure data is saved to the correct store.
    \item \textbf{AnalyticsService:} Does the math for the dashboard statistics.
    \item \textbf{ForecastService:} Communicates with the Python machine learning program.
\end{itemize}

\subsection{Database Design}
Important tables in our database include:
\begin{itemize}
    \setlength{\itemsep}{0pt}
    \setlength{\parskip}{0pt}
    \setlength{\topsep}{4pt}
    \item \textbf{Products:} Keeps track of the product name, price, and how many are in stock.
    \item \textbf{Orders:} Saves the total price and date of a sale.
    \item \textbf{OrderItems:} Connects an Order to specific Products and records the quantity sold.
    \item \textbf{Customers:} Saves customer contact details and their calculated loyalty scores.
\end{itemize}


\chapter{DESIGN IMPLEMENTATION}

\section{User Section}
In our system, users are divided into three roles to control what they can do:
\begin{itemize}
    \item Owner
    \item Manager
    \item Employee
\end{itemize}

Below are the details of how the main features were implemented in the background.

\noindent\begin{minipage}{\textwidth}
\subsection{Database Connection}
The application connects to the SQL Server database using Entity Framework Core. The connection details are safely stored in the \texttt{appsettings.json} file.
\begin{lstlisting}[language=json]
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost,1433;Database=SellWise;
    User Id=sa;Password=YourPass123!;TrustServerCertificate=True;"
  }
}
\end{lstlisting}
\end{minipage}
\vspace{1em}

\subsection{Login and Sign Up}
The authentication interface provides separate screens for existing users to log in and new users to create an account.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth,height=0.36\textheight,keepaspectratio]{diagrams/login_page.png}\\[0.15cm]
        \small Login screen
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth,height=0.36\textheight,keepaspectratio]{diagrams/signup_page.png}\\[0.15cm]
        \small Sign Up screen
    \end{minipage}
    \caption{Login and Sign Up User Interfaces}
\end{figure}

\noindent\begin{minipage}{\textwidth}
\subsection{Login and Sign Up Backend}
When a user logs in, ASP.NET Core Identity checks their password. If it is correct, the system saves their Store ID in a secure cookie. This makes sure that the user only sees data belonging to their specific store.
\begin{lstlisting}[language={[Sharp]C}]
[HttpPost]
public async Task<IActionResult> Login(
    LoginViewModel model)
{
    if (!ModelState.IsValid)
        return View(model);

    var result = await _signInManager
        .PasswordSignInAsync(
            model.Email!,
            model.Password!,
            isPersistent: true,
            lockoutOnFailure: false);

    if (result.Succeeded)
    {
        var user = await _userManager
            .FindByEmailAsync(model.Email!);

        // Redirect to dashboard
    }

    return View(model);
}
\end{lstlisting}
\end{minipage}
\vspace{1em}

\subsection{Dashboard}
The dashboard presents the main sales KPIs, interactive charts, and Prophet-based demand forecasts in one view.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.68\textheight,keepaspectratio]{diagrams/dashboard_page.png}
    \caption{Dashboard with KPIs, Sales Charts, and Demand Forecasts}
\end{figure}

\noindent\begin{minipage}{\textwidth}
\subsection{Dashboard Backend}
The analytics service calculates today's revenue, order counts, and pending orders for the active store to display on the dashboard.
\begin{lstlisting}[language={[Sharp]C}]
public async Task<(
    decimal TodayRevenue,
    int TodayOrdersCount,
    int PendingOrdersCount)> GetDashboardKpisAsync(
        Guid storeId)
{
    var today = DateTime.UtcNow.Date;

    var kpis = await _db.Orders
        .Where(o => o.StoreId == storeId)
        .GroupBy(o => 1)
        .Select(g => new
        {
            TodayRevenue = g
                .Where(o => o.OrderDate >= today &&
                            o.Status != "cancelled")
                .Sum(o => o.Total),
            TodayOrdersCount = g.Count(
                o => o.OrderDate >= today),
            PendingOrdersCount = g.Count(
                o => o.Status == "pending" ||
                     o.Status == "processing")
        })
        .FirstOrDefaultAsync();

    return (
        kpis?.TodayRevenue ?? 0m,
        kpis?.TodayOrdersCount ?? 0,
        kpis?.PendingOrdersCount ?? 0);
}
\end{lstlisting}
\end{minipage}
\vspace{1em}

\subsection{POS Order Screen}
The point-of-sale screen allows users to select products, manage the cart, choose a customer, and complete an order.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.68\textheight,keepaspectratio]{diagrams/pos_order_screen.png}
    \caption{POS Cart and Order Creation Screen}
\end{figure}

\noindent\begin{minipage}{\textwidth}
\subsection{Order Processing Backend}
To prevent errors when selling items, the system uses database transactions. This means that deducting the stock from the inventory and saving the payment record happen together.
\begin{lstlisting}[language={[Sharp]C}]
public async Task<string?> CreateOrderAsync(Guid storeId, OrderFormViewModel model)
{
    using var transaction = await _db.Database.BeginTransactionAsync();
    try
    {
        Guid? finalCustomerId = model.CustomerId;
        // ... (customer setup omitted for brevity) ...

        var order = new Order
        {
            StoreId = storeId,
            CustomerId = finalCustomerId,
            Status = "pending",
            OrderNumber = $"ORD-{DateTime.UtcNow:yyyyMMdd}-{Guid.NewGuid().ToString()[..6]}"
        };

        decimal subtotal = 0;
        var validItems = model.Items.Where(i => i.ProductId != Guid.Empty && i.Quantity > 0).ToList();

        foreach (var item in validItems)
        {
            var product = await _db.Products.FindAsync(item.ProductId);
            if (product == null || product.StockQuantity < item.Quantity)
            {
                await transaction.RollbackAsync();
                return $"Insufficient stock for {product?.Name ?? "product"}.";
            }
            product.StockQuantity -= item.Quantity; // Deduct stock safely
            // ...
        }
        await _db.SaveChangesAsync();
        await transaction.CommitAsync();
        return null;
    }
    catch (Exception ex)
    {
        await transaction.RollbackAsync();
        return "Failed to place order.";
    }
}
\end{lstlisting}
\end{minipage}
\vspace{1em}

\subsection{Inventory and Products}
The product catalog displays the available products and provides controls for maintaining prices, quantities, and other inventory information.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.68\textheight,keepaspectratio]{diagrams/inventory_products.png}
    \caption{Inventory Product Catalog}
\end{figure}

\noindent\begin{minipage}{\textwidth}
\subsection{Inventory and Products Backend}
When creating a new product, the system automatically assigns it to the current user's store and validates all input constraints before saving to the database.
\begin{lstlisting}[language={[Sharp]C}]
public async Task CreateProductAsync(
    Guid storeId,
    ProductFormViewModel model)
{
    var product = new Product
    {
        StoreId = storeId,
        Name = model.Name!,
        Sku = model.Sku,
        Category = model.Category!,
        Price = model.Price,
        StockQuantity = model.StockQuantity,
        IsActive = true
    };

    _db.Products.Add(product);
    await _db.SaveChangesAsync();
}
\end{lstlisting}
\end{minipage}
\vspace{1em}

\subsection{Inventory Alerts}
Low-stock and critical warning badges make products requiring immediate attention easy to identify.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.68\textheight,keepaspectratio]{diagrams/inventory_alerts.png}
    \caption{Low-Stock and Critical Inventory Alerts}
\end{figure}

\noindent\begin{minipage}{\textwidth}
\subsection{Inventory Alerts Backend}
A background task constantly checks the quantity of every product. If a product's stock drops below a set minimum limit, the system automatically creates a warning alert.
\begin{lstlisting}[language={[Sharp]C}]
public async Task ScanAndGenerateAlertsAsync(Guid storeId)
{
    // Remove alerts for products that are healthy
    var healthyOrInactiveProductIds = await _db.Products
        .Where(p => p.StoreId == storeId && (!p.IsActive || p.StockQuantity > p.LowStockThreshold))
        .Select(p => p.Id).ToListAsync();

    var alertsToRemove = await _db.Alerts
        .Where(a => a.StoreId == storeId && (healthyOrInactiveProductIds.Contains(a.ProductId) || a.Type.Contains("Resolved")))
        .ToListAsync();

    if (alertsToRemove.Any()) _db.Alerts.RemoveRange(alertsToRemove);

    // Generate alerts for low stock products
    var lowStockProducts = await _db.Products
        .Where(p => p.StoreId == storeId && p.IsActive && p.StockQuantity <= p.LowStockThreshold)
        .ToListAsync();
        
    // ... insert alerts logic ...
    await _db.SaveChangesAsync();
}
\end{lstlisting}
\end{minipage}
\vspace{1em}

\subsection{Customers}
The customer list displays customer information together with automatically calculated RFM segments, such as Champion and At Risk.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.68\textheight,keepaspectratio]{diagrams/customers.png}
    \caption{Customer List with RFM Segments}
\end{figure}

\noindent\begin{minipage}{\textwidth}
\subsection{Customer Segmentation (RFM) Backend}
SellWise automatically calculates a score for each customer using the RFM model: Recency, Frequency, and Monetary value. Based on this score, customers are grouped.
\begin{lstlisting}[language={[Sharp]C}]
public async Task RecalculateAllAsync(Guid storeId)
{
    var customers = await _db.Customers.Where(c => c.StoreId == storeId).ToListAsync();
    if (customers.Count == 0) return;

    var now = DateTime.UtcNow;
    var cutoffDate = now.AddDays(-365);

    var customerOrders = await _db.Orders
        .Where(o => o.StoreId == storeId && o.CustomerId.HasValue && o.OrderDate >= cutoffDate)
        .GroupBy(o => o.CustomerId!.Value)
        .Select(g => new
        {
            CustomerId = g.Key,
            LastOrderDate = g.Max(o => o.OrderDate),
            Frequency = g.Count(),
            Monetary = g.Sum(o => o.Total)
        }).ToListAsync();

    // Calculate quartiles and assign segments (e.g. "Champion", "At Risk")
    foreach (var customer in customers)
    {
        // Segment assignment logic based on Recency, Frequency, Monetary quartiles...
        customer.RfmSegment = DetermineSegment(rScore, fScore, mScore);
    }
    await _db.SaveChangesAsync();
}
\end{lstlisting}
\end{minipage}
\vspace{1em}

\noindent\begin{minipage}{\textwidth}
\subsection{Machine Learning Forecasting Backend}
We created a separate Python program (microservice) to handle predictions. It uses a library called Prophet to look at past sales and predict what will sell in the next 30 days.
\begin{lstlisting}[language=python]
@router.post("/forecast", response_model=ForecastResponse)
def create_forecast(request: ForecastRequest):
    try:
        # Generate forecast using the prophet service
        predictions = generate_forecast(
            request.history,
            request.periods
        )
        return ForecastResponse(
            store_id=request.store_id,
            product_id=request.product_id,
            forecast=predictions
        )
    except Exception as e:
        logger.exception("Forecast generation failed")
        raise HTTPException(
            status_code=500,
            detail="Forecast generation failed."
        )
\end{lstlisting}
\end{minipage}
\vspace{1em}

\subsection{Generated PDF Report}
SellWise can export a formatted PDF sales report so that users can save, print, or share business summaries.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.68\textheight,keepaspectratio]{diagrams/pdf_report.png}
    \caption{Generated PDF Sales Report}
\end{figure}

\noindent\begin{minipage}{\textwidth}
\subsection{PDF Generation Backend}
The system generates PDF reports dynamically using the QuestPDF library, providing a clean and easily downloadable summary of sales and orders.
\begin{lstlisting}[language={[Sharp]C}]
[Authorize]
public class ReportController : BaseController
{
    public ReportController(AppDbContext db)
        : base(db)
    {
        QuestPDF.Settings.License = LicenseType.Community;
    }

    public async Task<IActionResult> Index()
    {
        var storeId = GetCurrentStoreId();

        if (storeId == System.Guid.Empty)
        {
            return RedirectToAction("Login", "Auth");
        }

        var totalOrders = await Db.Orders
            .Where(o => o.StoreId == storeId)
            .CountAsync();

        var totalRevenue = await Db.Orders
            .Where(o => o.StoreId == storeId)
            .SumAsync(o => o.Total);

        // Render PDF view or return data
        return View();
    }
}
\end{lstlisting}
\end{minipage}
\vspace{1em}

\section{Conclusions}
A few important design choices helped make the system stable:
\begin{enumerate}
    \item Writing the business calculations in separate "Service" files kept the main web code clean and easy to read.
    \item Using database transactions for orders completely prevented issues where stock numbers did not match the sales records.
    \item Checking the Store ID for every database request ensured that data from one shop never mixed with another.
    \item Adding a backup calculation method means the dashboard still works even if the Python prediction service is turned off.
\end{enumerate}

\chapter{TESTING}

\section{Testing}
Testing is the process of running the software with test data to check if it works exactly as it is supposed to. During testing, we try to find errors so they can be fixed before the software is given to the final users.

\section{Validation Criteria}
The system was checked against the following rules:
\begin{itemize}
    \item User inputs must be checked for mistakes before being saved to the database.
    \item The design and colors of the website must look consistent on every page.
    \item If the system crashes during an update, the database must not be corrupted.
\end{itemize}

\section{Importance of Testing}
Testing is very important because it ensures the system is reliable. Finding and fixing bugs during the testing phase prevents the software from crashing when the shop owner is using it to run their business.

\section{Structural Testing}
Structural testing checks the internal code of the software. We reviewed the code to ensure that important database actions are placed inside \texttt{try-catch} blocks. This prevents the website from crashing completely if an error happens.

\section{Functional Testing}
Functional testing involves testing the features from the user's perspective. We manually tested logging in, adding products to the cart, processing orders, and checking if the dashboard numbers correctly updated after a sale.

\section{Acceptance Test Generation}
To see how the system handles a lot of data, we wrote a script to generate test data. We created two fake stores, added hundreds of customers, and generated 180 days of past orders. This helped us see how the system would perform in a real-world scenario.

\section{Unit Testing}
Unit testing focuses on checking small, individual parts of the code. For example, we tested the math behind the customer loyalty (RFM) calculation and the code that reads the prediction from the Python service to ensure they gave the correct results.

\section{Integration Testing}
Integration testing checks how different parts of the system work together. We specifically tested the connection between the main C\# web application and the Python machine learning service to make sure they communicated properly.

\section{Test Data Used}
\begin{itemize}
    \item \textbf{Live Test Data:} We manually typed in real examples of products and sales to test the website forms.
    \item \textbf{Artificial Test Data:} We used automated scripts to enter thousands of fake orders to test if the database slowed down.
\end{itemize}

\section{Test Cases}
\begin{table}[H]
\centering
\begin{tabular}{|l|p{6cm}|l|l|}
\hline
\textbf{Module} & \textbf{Test Case Scenarios} & \textbf{Tested} & \textbf{Working} \\
\hline
Authentication & 1. Check login with correct password\newline 2. Check account locks after 5 wrong attempts\newline 3. Check if user is restricted to their own store & Yes & Yes \\
\hline
Dashboard & 1. Verify the sales statistics are correct\newline 2. Check if the graphs show the right data & Yes & Yes \\
\hline
POS Orders & 1. Check adding and removing items in the cart\newline 2. Verify an order cannot be placed without enough stock\newline 3. Verify the transaction cancels safely on error & Yes & Yes \\
\hline
Products & 1. Test adding, editing, and deleting a product\newline 2. Check if low stock alerts appear correctly & Yes & Yes \\
\hline
Customers & 1. Verify the loyalty score calculation is accurate\newline 2. Verify customers are assigned the correct group & Yes & Yes \\
\hline
\end{tabular}
\caption{Test Cases and Results}
\end{table}

\section{Testing Result}
The login system worked perfectly, showing warnings for wrong passwords and locking the account after too many tries. The order processing system also worked safely, correctly deducting stock only when the sale was finalized.

The following screenshots demonstrate validation and error handling during functional testing.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth,height=0.65\textheight,keepaspectratio]{diagrams/test_wrong_password.png}
    \caption{Invalid Password Warning During Login Testing}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth,height=0.65\textheight,keepaspectratio]{diagrams/test_out_of_stock.png}
    \caption{Out-of-Stock Validation During Order Testing}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth,height=0.65\textheight,keepaspectratio]{diagrams/test_product_error.png}
    \caption{Product Input Validation Error}
\end{figure}

\section{Testing Summary}
We tested all the pages available to the Owner, Manager, and Employee. We checked the login process, order creation, and report generation. After finding and fixing a few minor display issues, the project passed all tests and is working smoothly.


\chapter{USER MANUAL}

\section{Introduction}
The SellWise User Manual is designed to help shop owners, managers, and employees quickly learn how to use the system. SellWise provides a comprehensive set of tools to manage a retail shop by combining point-of-sale, inventory tracking, and sales forecasting into one accessible platform. Because it is a web-based application, it offers the flexibility to monitor your sales and inventory from any device on your network.

This chapter covers the system requirements necessary to run the application and provides a step-by-step guide to using the core features of the user interface.

\section{System Requirements}

Because SellWise is a web application, the system requirements are divided into two categories: what the server needs to host the application, and what the end-user needs to access it.

\subsection{Server-Side Requirements (Hosting \& Deployment)}
To host the SellWise application, run the machine learning forecasts, and manage the database, the server environment must meet the following specifications:
\begin{itemize}
    \item \textbf{Operating System:} Windows 10/11, macOS, or a modern Linux distribution (e.g., Ubuntu).
    \item \textbf{Hardware:} Minimum Dual-Core processor with 8GB of RAM (required to smoothly run Docker and the Python environment simultaneously). At least 50GB of free storage.
    \item \textbf{Database Engine:} Docker Engine (or Docker Desktop) is required to run the Microsoft SQL Server 2022 container.
    \item \textbf{Web Framework:} .NET 10.0 SDK/Runtime to host the main ASP.NET Core MVC application.
    \item \textbf{Machine Learning Environment:} Python 3.11 or higher, configured with the FastAPI framework and the Meta Prophet library for sales forecasting.
\end{itemize}

\subsection{Client-Side Requirements (End-Users)}
For shop owners, managers, and employees, no installation of the software is required. They only need the following to use the system:
\begin{itemize}
    \item \textbf{Hardware:} Any basic PC, laptop, or tablet.
    \item \textbf{Operating System:} Any OS (Windows, macOS, Linux, Android, iOS).
    \item \textbf{Web Browser:} A modern, up-to-date web browser such as Google Chrome, Mozilla Firefox, Safari, or Microsoft Edge.
    \item \textbf{Network:} A stable internet or local area network (LAN) connection to communicate with the SellWise web server.
\end{itemize}

\section{User Interface Guide}

The user interface of SellWise was built using Bootstrap 5 to ensure it is clean, intuitive, and mobile-friendly. Below is a guide to navigating the primary sections of the application.

\subsection{Getting Started and Authentication}
When you launch the system, the first page you encounter is the \textbf{User Login} screen. This page restricts access so that only authorized personnel can view sensitive store data. You must enter your registered email address and password. If you do not have an account, the \textbf{Sign Up} page allows you to register and automatically creates a new isolated store workspace for you.

\subsection{Dashboard and Analytics}
Immediately after logging in, you will be directed to the \textbf{Dashboard}. This is the central hub for monitoring your business.
\begin{itemize}
    \item \textbf{KPI Cards:} At the top, you will see today's total revenue, the number of orders processed today, and any pending orders.
    \item \textbf{Sales Charts:} Interactive graphs show your sales performance over the past week and month.
    \item \textbf{Machine Learning Forecast:} A dedicated chart displays the Prophet-based prediction of your product demand for the next 30 days, helping you decide when to restock.
\end{itemize}

\subsection{Point of Sale (Orders)}
To process a customer's purchase, navigate to the \textbf{Orders} section. 
\begin{itemize}
    \item You can search for products using the search bar and click to add them to the current cart.
    \item You can adjust quantities and apply discounts or delivery charges.
    \item Before finalizing the sale, you can select an existing customer from the dropdown or quickly register a new customer.
    \item Clicking "Complete Order" will safely deduct the items from your inventory and log the sale.
\end{itemize}

\subsection{Inventory and Alerts}
To manage your stock, go to the \textbf{Products} page. Here, you can view your entire catalog, add new items, and update prices or stock quantities. If an item's stock falls below its defined minimum threshold, it will trigger an alert. You can view these critical warnings on the \textbf{Inventory Alerts} page, ensuring you never run out of popular items unexpectedly.

\subsection{Customer Management}
The \textbf{Customers} page displays a comprehensive directory of your buyers. Alongside their contact information, the system displays their automatically calculated RFM segment (such as "Champion", "Loyal", or "At Risk"). This helps you identify which customers to reward and which ones you might need to re-engage.

\section{Conclusion}
This chapter detailed the hardware and software prerequisites needed to operate SellWise successfully. It also provided a foundational walkthrough of the user interface, outlining how to perform daily operations from processing sales to analyzing AI-driven forecasts. The intuitive design ensures that shop employees require minimal training to become proficient.


\chapter{CONCLUSION AND FUTURE WORKS}

\section{Conclusion}
The SellWise project was developed to address a significant operational gap in the retail sector: small and medium-sized shop owners desperately need modern software to manage their businesses, but existing enterprise tools are often too complicated, bloated, or expensive. Throughout the lifecycle of this project, we successfully engineered a complete, full-stack web application that handles point-of-sale operations, tracks inventory in real-time, automatically segments customers, and leverages machine learning to predict future sales.

Building this system highlighted the importance of a clean architectural design. By enforcing strict multi-tenancy at the database level and handling financial transactions safely through Entity Framework Core, we ensured that the system is both secure and reliable. Furthermore, separating the computationally heavy Python prediction code into an isolated microservice guaranteed that the main C\# web application remained fast and responsive for cashiers processing live orders. 

\section{Outcomes}
The successful implementation of SellWise yielded several positive outcomes:
\begin{itemize}
    \item \textbf{Centralized Management:} Shop owners can now visualize all their critical business data on a single, unified dashboard, drastically reducing the time spent compiling paper records or managing disparate spreadsheets.
    \item \textbf{Proactive Restocking:} The integration of the Meta Prophet machine learning model helps owners accurately forecast upcoming product demand, preventing stockouts of popular items and reducing the financial burden of overstocking.
    \item \textbf{Automated Customer Insights:} By automatically calculating RFM scores, the system instantly identifies the most valuable customers without requiring the owner to perform complex data analysis manually.
    \item \textbf{Data Integrity:} The robust order processing pipeline ensures that inventory numbers are strictly tied to finalized sales, eliminating the discrepancies that plague manual systems.
    \item \textbf{Early Warnings:} Automated background tasks continuously scan the database to provide managers with early warning alerts before an item reaches critical stock levels.
\end{itemize}

\section{Current Limitations}
While the system fulfills its primary objectives exceptionally well, we have identified a few limitations during testing that should be noted:
\begin{itemize}
    \item \textbf{Single ML Model Dependency:} The demand forecasting currently relies entirely on the Prophet library. While effective for general seasonal trends, the system does not evaluate or compare alternative machine learning models (such as ARIMA or neural networks) that might yield higher accuracy for highly volatile products.
    \item \textbf{Network Dependency:} SellWise is a web-based application requiring an active network connection to process sales. There is currently no localized offline mode to queue transactions if the internet or local server connection drops.
    \item \textbf{Single Currency Constraint:} A registered store is bound to a single type of currency and taxation rule. This could present challenges for shops operating near international borders that accept multiple currencies.
\end{itemize}

\section{Future Works}
To further evolve SellWise into an enterprise-grade retail platform, several enhancements are planned for future development iterations:
\begin{itemize}
    \item \textbf{Automated Marketing Integrations:} We plan to integrate the RFM customer segmentation data directly with email marketing APIs (such as SendGrid or Mailchimp). This would allow the system to automatically send promotional codes or "We miss you" emails to customers categorized as "At Risk".
    \item \textbf{Mobile Application Counterpart:} Developing a companion Android and iOS application using frameworks like Flutter or .NET MAUI would empower managers to review dashboard analytics remotely and allow employees to process inventory checks using their smartphone cameras as barcode scanners.
    \item \textbf{Cloud Deployment Architecture:} Transitioning the current Docker-based local setup into a fully managed cloud architecture (using services like AWS ECS or Azure App Services) would enhance scalability, provide automated off-site backups, and drastically simplify the onboarding process for new store owners.
    \item \textbf{Multi-Branch Support:} Enhancing the system to support a single owner managing shared inventory across multiple geographic store branches, allowing for inter-store stock transfers.
\end{itemize}

\end{document}